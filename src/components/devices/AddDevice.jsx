import styles from "./AddDevice.module.scss"
import classNames from "classnames"
import Modal from "../modal/Modal"

export default function AddDevice({ searching, found, failed, onDevice }) {
    return (
        <Modal>

        </Modal>
    )
}